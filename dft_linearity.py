import numpy as np
import matplotlib.pyplot as plt
import cmath as cm
j=cm.sqrt(-1)
def dft(x,N)
	c=0
	f=[]
	for n in range(p)
		for k in range(p)
			c=c+(x[k]*np.exp((-(j*2*np.pi*k*l/N))))
		r.append(c)
		c=0
	return r

